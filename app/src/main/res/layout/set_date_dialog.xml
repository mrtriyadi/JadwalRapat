<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    app:layout_constraintWidth_percent="0.75"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="20sp"
        android:layout_margin="10dp"
        android:text="Silahkan Pilih Tanggal"
        android:textAlignment="center"
        />

    <DatePicker
        android:id="@+id/dt_tambah_rapat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="@id/tv_title"
        app:layout_constraintRight_toRightOf="@id/tv_title"
        app:layout_constraintTop_toBottomOf="@id/tv_title"
        />

    <Button
        android:id="@+id/btn_set_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/dt_tambah_rapat"
        app:layout_constraintLeft_toLeftOf="@id/dt_tambah_rapat"
        app:layout_constraintRight_toRightOf="@id/dt_tambah_rapat"
        style="@style/Widget.AppCompat.DropDownItem.Spinner"
        android:text="Pilih Tanggal Rapat"
    />
</androidx.constraintlayout.widget.ConstraintLayout>